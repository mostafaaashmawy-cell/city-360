# Cloudflare Workers static assets deployment for City 360
# `wrangler deploy` with [assets] serves the out/ folder as a static Worker site

name = "city-360"
compatibility_date = "2024-09-23"

[assets]
directory = "./out"

